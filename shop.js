const cfg=window.ELEVEN_STORE||{},$=s=>document.querySelector(s);
const esc=s=>String(s??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");
const money=n=>`${cfg.currency||"SAR"} ${Number(n||0).toLocaleString("en-US",{maximumFractionDigits:2})}`;
let allProducts=[],currentCategory="all",currentPrice="all",sortMode="newest",searchValue="";
function cart(){try{return JSON.parse(localStorage.getItem("eleven_cart")||"[]")}catch{return[]}}
function save(c){localStorage.setItem("eleven_cart",JSON.stringify(c));count()}
function count(){const n=cart().reduce((a,x)=>a+Number(x.qty||1),0);$("#cartCount").textContent=n}
function add(p){const c=cart(),x=c.find(i=>Number(i.id)===Number(p.id)&&!i.option);if(x)x.qty++;else c.push({id:p.id,name:p.name,price:p.price,image:p.image||"",qty:1});save(c);location.href="/cart"}
function categoryOf(p){if(p.productType==="programming_service")return "services";const t=String(p.tag||"").toLowerCase();if(t.includes("script")||t.includes("mt")||t.includes("resource"))return "scripts";return "other"}
function filtered(){let rows=[...allProducts];if(currentCategory!=="all")rows=rows.filter(p=>categoryOf(p)===currentCategory);if(currentPrice==="free")rows=rows.filter(p=>Number(p.price||0)===0);if(currentPrice==="paid")rows=rows.filter(p=>Number(p.price||0)>0||p.productType==="programming_service");if(searchValue)rows=rows.filter(p=>`${p.name} ${p.description||""} ${p.tag||""}`.toLowerCase().includes(searchValue));if(sortMode==="price-low")rows.sort((a,b)=>Number(a.price||0)-Number(b.price||0));else if(sortMode==="price-high")rows.sort((a,b)=>Number(b.price||0)-Number(a.price||0));else rows.sort((a,b)=>Number(a.sortOrder||0)-Number(b.sortOrder||0)||Number(b.id)-Number(a.id));return rows}
function render(){const products=filtered();$("#productCount").textContent=products.length;const grid=$("#products");grid.innerHTML=products.map(p=>{const isService=p.productType==="programming_service";const priceText=isService?`From ${money(Number(p.servicePricing?.hourlyRate||0))}/hr`:(Number(p.price||0)===0?"FREE":money(p.price));return `<article class="product-card"><button class="product-image product-open" data-open-id="${p.id}"><img class="${p.image?'':'logo-fallback'}" src="${p.image?esc(p.image):'/assets/eleven-logo.png'}"><span class="preview-badge">PREVIEW</span><span class="product-badges"><span class="${isService?'badge-service':'badge-new'}">${isService?'SERVICE':'NEW'}</span></span></button><div class="product-body"><button class="product-title product-open" data-open-id="${p.id}">${esc(p.name)}</button><div class="product-meta"><span>◫ Eleven Store</span><span>${Array.isArray(p.previewImages)?p.previewImages.length+1:1} images</span></div><p>${esc(p.description||"")}</p><div class="product-bottom"><strong>${priceText}</strong><button class="add-btn" data-id="${p.id}">${isService?'Choose Plan':'Add'}</button></div></div></article>`}).join("")||'<div class="empty">No products found.</div>';grid.onclick=e=>{const open=e.target.closest(".product-open");if(open){const p=allProducts.find(x=>Number(x.id)===Number(open.dataset.openId));if(p)openProductPreview(p);return}const b=e.target.closest(".add-btn");if(!b)return;const p=allProducts.find(x=>Number(x.id)===Number(b.dataset.id));if(!p)return;if(p.productType==="programming_service")openServiceModal(p);else add(p)}}
function setSearch(v){searchValue=String(v||"").toLowerCase().trim();render()}
async function loadNotifications(){try{const r=await fetch("/api/notifications"),rows=await r.json();if(!Array.isArray(rows)||!rows.length)return;const latest=rows[0],dismissed=localStorage.getItem("eleven_notice_"+latest.id);if(dismissed)return;$("#announcementText").textContent=latest.message;$("#announcementBar").classList.remove("hidden");$("#closeAnnouncement").onclick=()=>{localStorage.setItem("eleven_notice_"+latest.id,"1");$("#announcementBar").classList.add("hidden")}}catch{}}
async function load(){$("#year").textContent=new Date().getFullYear();count();const r=await fetch("/api/products");allProducts=await r.json();$("#countAll").textContent=allProducts.length;$("#countScripts").textContent=allProducts.filter(p=>categoryOf(p)==="scripts").length;$("#countServices").textContent=allProducts.filter(p=>categoryOf(p)==="services").length;$("#countOther").textContent=allProducts.filter(p=>categoryOf(p)==="other").length;render();loadNotifications();$("#searchInput").oninput=e=>setSearch(e.target.value);$("#sideSearch").oninput=e=>{if($("#searchInput"))$("#searchInput").value=e.target.value;setSearch(e.target.value)};$("#sortSelect").onchange=e=>{sortMode=e.target.value;render()};document.querySelectorAll("[data-category]").forEach(btn=>btn.onclick=()=>{document.querySelectorAll("[data-category]").forEach(x=>x.classList.remove("active"));btn.classList.add("active");currentCategory=btn.dataset.category;render()});document.querySelectorAll("[data-price-filter]").forEach(btn=>btn.onclick=()=>{document.querySelectorAll("[data-price-filter]").forEach(x=>x.classList.remove("active"));btn.classList.add("active");currentPrice=btn.dataset.priceFilter;render()})}
function openProductPreview(p){let modal=document.querySelector("#productPreviewModal");if(!modal){modal=document.createElement("div");modal.id="productPreviewModal";modal.className="product-preview-modal";document.body.appendChild(modal)}const gallery=[...(p.image?[p.image]:[]),...(Array.isArray(p.previewImages)?p.previewImages:[])];if(!gallery.length)gallery.push("/assets/eleven-logo.png");let index=0;modal.innerHTML=`<div class="preview-backdrop" data-close-preview></div><div class="preview-dialog"><button class="preview-close" data-close-preview>×</button><div class="preview-gallery"><div class="preview-main-wrap"><button class="preview-nav prev">‹</button><img id="previewMainImage" src="${esc(gallery[0])}"><button class="preview-nav next">›</button></div><div class="preview-thumbs">${gallery.map((u,i)=>`<button data-preview-index="${i}" class="${i===0?'active':''}"><img src="${esc(u)}"></button>`).join("")}</div></div><div class="preview-info"><span class="eyebrow">${esc(p.tag||"ELEVEN")}</span><h2>${esc(p.name)}</h2><p>${esc(p.description||"")}</p><div class="preview-info-bottom"><strong>${p.productType==="programming_service"?`From ${money(Number(p.servicePricing?.hourlyRate||0))}/hr`:(Number(p.price||0)===0?"FREE":money(p.price))}</strong><button id="previewAddToCart" class="add-btn">${p.productType==="programming_service"?"Choose Plan":"Add to cart"}</button></div></div></div>`;const main=modal.querySelector("#previewMainImage"),thumbs=modal.querySelectorAll("[data-preview-index]");const show=i=>{index=(i+gallery.length)%gallery.length;main.src=gallery[index];thumbs.forEach((t,ti)=>t.classList.toggle("active",ti===index))};modal.querySelector(".prev").onclick=()=>show(index-1);modal.querySelector(".next").onclick=()=>show(index+1);thumbs.forEach(t=>t.onclick=()=>show(Number(t.dataset.previewIndex)));modal.querySelectorAll("[data-close-preview]").forEach(x=>x.onclick=()=>modal.classList.remove("show"));modal.querySelector("#previewAddToCart").onclick=()=>{if(p.productType==="programming_service"){modal.classList.remove("show");openServiceModal(p)}else add(p)};modal.classList.add("show")}
function openServiceModal(p){let modal=document.querySelector("#serviceModal");if(!modal){modal=document.createElement("div");modal.id="serviceModal";modal.className="service-modal";document.body.appendChild(modal)}const sp=p.servicePricing||{};modal.innerHTML=`<div class="service-backdrop" data-close-service></div><div class="service-dialog"><button class="service-close" data-close-service>×</button><span class="eyebrow">PROGRAMMING SERVICE</span><h2>${esc(p.name)}</h2><p>${esc(p.description||"")}</p><div class="service-tabs"><button class="active" data-service-tab="hours">Hourly</button><button data-service-tab="plans">Plans</button></div><div id="serviceHours"><div class="hours-grid">${[1,2,3,4].map(h=>`<button class="service-option" data-option="hours:${h}" data-price="${Number(sp.hourlyRate||0)*h}">${h} Hour${h>1?'s':''}<strong>${money(Number(sp.hourlyRate||0)*h)}</strong></button>`).join("")}</div></div><div id="servicePlans" class="hidden"><div class="plan-grid"><button class="service-option" data-option="weekly" data-price="${Number(sp.weekly||0)}">Weekly<strong>${money(sp.weekly||0)}</strong></button><button class="service-option" data-option="half_monthly" data-price="${Number(sp.halfMonthly||0)}">Half Monthly<strong>${money(sp.halfMonthly||0)}</strong></button><button class="service-option" data-option="monthly" data-price="${Number(sp.monthly||0)}">Monthly<strong>${money(sp.monthly||0)}</strong></button><button class="service-option" data-option="yearly" data-price="${Number(sp.yearly||0)}">Yearly<strong>${money(sp.yearly||0)}</strong></button></div></div><div class="service-summary"><span>Selected</span><strong id="serviceSelected">Choose an option</strong></div><button id="serviceAdd" class="add-btn" disabled>Add to cart</button></div>`;let selectedOption=null,selectedPrice=0,selectedLabel="";modal.querySelectorAll("[data-close-service]").forEach(x=>x.onclick=()=>modal.classList.remove("show"));modal.querySelectorAll("[data-service-tab]").forEach(btn=>btn.onclick=()=>{modal.querySelectorAll("[data-service-tab]").forEach(x=>x.classList.remove("active"));btn.classList.add("active");modal.querySelector("#serviceHours").classList.toggle("hidden",btn.dataset.serviceTab!=="hours");modal.querySelector("#servicePlans").classList.toggle("hidden",btn.dataset.serviceTab!=="plans")});modal.querySelectorAll(".service-option").forEach(btn=>btn.onclick=()=>{modal.querySelectorAll(".service-option").forEach(x=>x.classList.remove("selected"));btn.classList.add("selected");selectedOption=btn.dataset.option;selectedPrice=Number(btn.dataset.price||0);selectedLabel=btn.childNodes[0].textContent.trim();modal.querySelector("#serviceSelected").textContent=`${selectedLabel} · ${money(selectedPrice)}`;modal.querySelector("#serviceAdd").disabled=selectedPrice<=0});modal.querySelector("#serviceAdd").onclick=()=>{if(!selectedOption||selectedPrice<=0)return;const c=cart();c.push({id:p.id,name:`${p.name} - ${selectedLabel}`,price:selectedPrice,image:p.image||"",qty:1,option:selectedOption});save(c);location.href="/cart"};modal.classList.add("show")}
load().catch(console.error);